// ATENÇÃO: A LÓGICA DESTE ARQUIVO FOI MOVIDA PARA UM SERVIDOR BACK-END.
//
// Para proteger a chave de API, ela nunca deve ser usada diretamente no código do front-end (o código que roda no navegador).
// Em vez disso, o aplicativo agora faz uma chamada para seu próprio servidor, e é o servidor que, de forma segura,
// usa a chave de API para se comunicar com a API do Google.
//
// Veja o exemplo de código do servidor na resposta da conversa.
